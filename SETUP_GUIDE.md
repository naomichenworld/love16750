# O.HOME 배포 가이드

자캐놀이용 개인홈을 내 계정에 설치하고 운영하는 방법

---

## 0. 어떤 홈이 되나

압축을 풀어 올리고, 접속해서 설치 화면을 한 번 거치면 **방문자에게 내 글과 그림이 보이는 공개 홈**이 됩니다.
데이터는 브라우저가 아니라 **직접 만든 데이터베이스**에 저장됩니다.

데이터베이스는 둘 중 하나를 고릅니다. 설치 화면에서 선택합니다.

| | Supabase | Firebase |
|---|---|---|
| 성격 | Postgres(표 형태) | Firestore(문서 형태) |
| 무료 저장 | DB 약 500MB · 이미지 약 1GB | DB 약 1GB · **이미지 약 5GB** |
| 요금 | 무료 다음 단계가 월 $25 | 고정비 없이 사용량 과금 (개인 홈 규모면 대개 0원) |
| 특징 | 7일간 접속이 없으면 프로젝트 일시정지 | Storage 사용 시 종량제(Blaze) 전환 필요 — 무료 한도 안에서는 청구 0원 |
| 이럴 때 | 글 위주, 그림은 적당히 | **그림이 많은 홈** |

> 용량·요금 정책은 바뀝니다. 결제 전에 각 서비스에서 최신 내용을 확인하세요.
> 나중에 마음이 바뀌어도 **환경설정 → 데이터 백업 → 데이터베이스 이전**으로 통째로 옮길 수 있습니다(서로 반대 방향도 됩니다).

---

## 1. 준비물

| 항목 | 조건 |
|---|---|
| Node.js | **20.9 이상** (권장 22 LTS 이상) — `node -v` |
| GitHub 계정 | 무료 |
| Vercel 계정 | 무료(Hobby) |
| Supabase 또는 Firebase 계정 | 무료로 시작 |

---

## 2. 올리기

### 「포크」가 뭔가요

**저장소를 통째로 내 계정에 복사해 오는 것**입니다. GitHub 저장소 페이지 오른쪽 위의
[Fork] 버튼 하나면 끝나고, 복사본은 온전히 내 것이라 마음대로 고칠 수 있습니다.

포크의 진짜 이점은 **원본과 연결이 남는다**는 점입니다.
나중에 원본에 새 기능이 올라오면 내 저장소 페이지에 [Sync fork] 버튼이 나타나고,
그걸 누르면 **내 홈이 알아서 새 버전으로 다시 배포**됩니다 — 글도 테마도 그대로 둔 채로요.

아래 2-A는 나눠 주는 사람이 한 번만 하는 준비이고, 실제 설치하는 사람은 2-B만 보면 됩니다.

---

### 2-A. 나눠 주는 사람이 미리 할 일 (한 번만)

1. **코드를 GitHub에 올리고 저장소를 공개(Public)로 둡니다.**
   비공개 저장소는 남이 포크할 수 없습니다.

2. **연결 정보 파일이 올라가지 않았는지 확인합니다.**
   `public/ohome.config.json`은 내 데이터베이스 주소·키라, 올라가면
   **설치한 사람 전부가 내 DB에 붙습니다.** `.gitignore`에 걸려 있으니
   강제로 추가만 하지 않으면 됩니다.

   ```bash
   git ls-files public/     # 목록에 ohome.config.json이 없어야 정상
   ```

3. **저장소 주소를 알려 줍니다.** 받는 사람은 그 페이지에서 [Fork]를 누르면 됩니다.

4. 새 기능을 만들면 **여기에 올리기만** 하면 됩니다.
   받은 사람이 [Sync fork]를 누를 때 각자의 홈에 반영됩니다 —
   내가 남의 홈을 마음대로 바꾸는 일은 일어나지 않습니다.

---

### 2-B. 설치하는 사람이 하는 일

**① 저장소를 내 계정으로 가져오기**

**GitHub 계정이 없다면 먼저 만듭니다** — [github.com/signup](https://github.com/signup), 무료이고 1분이면 됩니다.
코드를 보관하는 곳이라 홈을 운영하는 내내 쓰게 되고, 다음 단계의 Vercel 가입에도 이 계정을 씁니다.

계정이 준비되면 받은 저장소 주소로 들어가 오른쪽 위 **[Fork]** → **[Create fork]** 를 누릅니다.
내 계정에 똑같은 저장소가 생깁니다.

**② Vercel 계정 만들기**

Vercel은 홈을 인터넷에 띄워 주는 곳입니다. 따로 가입할 필요 없이 **방금 만든 GitHub 계정으로 시작**합니다.

1. [vercel.com/signup](https://vercel.com/signup) 접속
2. **[Continue with GitHub]** 클릭 — 이게 가입이자 로그인입니다
3. GitHub 로그인 화면이 뜨면 로그인하고, **[Authorize Vercel]** 로 허용합니다
   - Vercel이 내 저장소를 읽어 배포하기 위해 필요한 권한입니다
4. 요금제를 물으면 **Hobby(개인용, 무료)** 를 고릅니다

**③ GitHub 저장소를 Vercel에 연결하기**

로그인하면 **Import Git Repository** 화면이 나옵니다. **처음 쓰는 분들이 가장 많이 막히는 지점**이라 자세히 적습니다.

- 저장소 목록이 **비어 있거나**, 목록 대신 **[Continue with GitHub]** 버튼만 보인다면 아직 GitHub이 연결되지 않은 것입니다 → **그 버튼을 누릅니다**
- **[Install]** 화면에서 Vercel이 접근할 저장소를 고릅니다
  - **All repositories** — 전부 허용 (신경 쓸 게 없어 간단합니다)
  - **Only select repositories** — 포크한 저장소 하나만 골라도 됩니다
- 설치를 마치면 목록에 저장소가 나타납니다

> 이미 연결했는데 저장소가 안 보이면 **[Adjust GitHub App Permissions]** 에서 접근 범위에 그 저장소를 추가하세요.

**④ 배포하기**

1. 목록에서 포크한 저장소를 찾아 **[Import]** 클릭
2. 설정 화면이 나와도 **아무것도 바꾸지 말고** **[Deploy]** 클릭
3. 1~3분 기다립니다
4. 완료되면 **`https://내프로젝트.vercel.app`** 주소가 나옵니다. 눌러서 들어가면 **설치 화면**이 뜹니다 (3장으로)

이 과정에서 **환경변수는 건드리지 않습니다.** 데이터베이스 연결은 다음 장의 설치 화면에서 합니다.
내 도메인은 나중에 Vercel 프로젝트의 **Settings → Domains**에서 연결하면 됩니다.

**④ 서버 위치를 서울로 — 직접 바꿔야 합니다 (한국에서 쓴다면 꼭)**

Vercel은 서버 함수를 **미국 동부(iad1)** 에 두는 것이 기본값입니다.
그냥 두면 페이지를 열 때마다 태평양을 왕복해 첫 응답이 200~300ms씩 늦습니다.

**저장소 설정으로는 바뀌지 않습니다.** (`vercel.json`에 리전을 적어 봤지만 새 프로젝트에서도 적용되지 않는 것을 확인했습니다.) 아래 화면에서 직접 바꿔 주세요 — 30초면 됩니다.

```
Vercel 프로젝트 → Settings → Functions
   → Function Region → Seoul (icn1) 선택 → [Save]

그 다음 반드시:
Deployments → 맨 위 배포의 ⋯ → [Redeploy]
```

**저장만 하면 안 바뀝니다.** 이미 떠 있는 배포는 예전 리전 그대로라, **재배포를 해야** 서울로 옮겨집니다.

**적용됐는지 확인** — 홈에서 `F12` → **Network** 탭 → 맨 위 문서 요청 → **Response Headers** 의 `x-vercel-id`

| 값 | 뜻 |
|---|---|
| `icn1::…` | 서울 ✅ |
| `iad1::…` | 미국 동부 — 아직 안 바뀜 |

> 이미지·CSS·JS 같은 파일은 원래 전 세계 CDN에서 오므로 이 설정과 무관하게 빠릅니다.
> 리전이 바꾸는 것은 **페이지 첫 응답 속도**입니다.

**⑤ 나중에 새 버전 받기**

내 저장소 페이지 위쪽의 **[Sync fork]** → **[Update branch]** 를 누르면 끝입니다.
Vercel이 1~2분 안에 새로 배포하고, **글·이미지·테마·회원은 내 데이터베이스에 있으므로 그대로**입니다.

> 포크 대신 한 번에 배포하는 링크(`vercel.com/new/clone?repository-url=…`)도 있지만,
> 그렇게 만들어진 저장소는 원본과 연결이 끊긴 복사본이라 **[Sync fork]로 업데이트를 받을 수 없습니다.**

---

### 링크 없이 zip만 받았다면

압축을 풀고 그 폴더에서 아래 두 줄이면 GitHub 없이도 배포됩니다.

```bash
npm install
npx vercel        # 안내에 따라 로그인 → 질문에 Enter만 눌러도 기본값으로 배포
```

직접 GitHub에 올리고 싶다면 저장소를 만들어 올린 뒤
Vercel에서 **Add New → Project → Import**를 골라도 결과는 같습니다.

> 이 홈은 주소마다 서버가 페이지를 만들어 주는 방식이라 **정적 전용 호스팅(FTP·GitHub Pages)에는 그대로 올라가지 않습니다.**

---

## 3. 설치 화면 — 데이터베이스 연결

배포 주소에 처음 들어가면 설치 화면이 뜹니다. **Supabase / Firebase 중 하나를 고르면** 그에 맞는 안내가 나옵니다.

**먼저 알아두실 것 — 나중에 못 바꾸는 선택이 있습니다.** 아래 표만 봐 두고 시작하세요.

| 어디서 | 항목 | 고를 값 |
|---|---|---|
| Supabase | Region | **Northeast Asia (Seoul)** |
| Firebase | Firestore 에디션 | **Standard** |
| Firebase | Firestore 위치 | **asia-northeast3** (서울) |
| Firebase | Storage 위치 | 무료 유지면 **us-west1** · 속도 우선이면 **asia-northeast3** |

위치를 잘못 골랐더라도 데이터를 잃지는 않습니다 — 새 프로젝트를 만들고 환경설정의 **데이터베이스 이전**으로 통째로 옮길 수 있습니다. 다만 번거로우니 처음에 맞춰 두는 게 좋습니다.

### Supabase를 고른 경우

#### ① 프로젝트 만들기

[supabase.com/dashboard](https://supabase.com/dashboard) → **[New project]**

- **Name** — 아무거나
- **Database Password** — **[Generate a password]** 로 만들고 **어딘가에 반드시 저장해 두세요.** 홈을 쓰는 데는 필요 없지만, 나중에 데이터베이스에 직접 접속할 일이 생기면 이 비밀번호가 있어야 하고 **다시 볼 수 없습니다**
- **Region** — **`Northeast Asia (Seoul)`** 을 고르세요

> **리전은 무료 요금제에서도 자유롭게 고를 수 있고, 서울이 가장 빠릅니다.** (Firebase의 이미지 저장소와 달리 미국을 골라야 할 이유가 없습니다.) 다만 **나중에 바꿀 수 없으니** 여기서 서울을 고르세요.

만들어지는 데 1~2분 걸립니다.

#### ② 연결 정보 가져오기

왼쪽 아래 **⚙️ Project Settings** → **API** (또는 **API Keys**)

| 화면의 항목 | 설치 화면에 넣을 곳 |
|---|---|
| **Project URL** | 주소 칸 |
| **anon** / **public** 키 | 키 칸 |

> ⚠️ **`service_role` 키는 절대 넣지 마세요.** 모든 권한을 무시하는 관리자 키라, 공개된 홈에 넣으면 누구나 데이터를 지울 수 있습니다. 설치 화면이 그 키를 감지하면 막아 주긴 하지만 아예 복사하지 않는 게 좋습니다.

#### ③ 테이블 만들기

설치 화면의 **[SQL 복사]** → Supabase 왼쪽 메뉴 **SQL Editor** → 붙여넣고 **[Run]**

테이블 18종·권한(RLS)·이미지 저장소가 한 번에 만들어집니다. 「Success」가 뜨면 된 겁니다.

#### ④ 마무리

1. 설치 화면 **[연결 확인]**
2. **관리자 계정 만들기** — 여기서 만드는 첫 계정이 관리자가 됩니다

### Firebase를 고른 경우

선택지가 여러 번 나오는데, **한 번 고르면 못 바꾸는 항목이 두 개**(Firestore 위치·Storage 위치) 있습니다. 아래 순서대로 따라오시면 됩니다.

#### ① 프로젝트 만들기

[Firebase 콘솔](https://console.firebase.google.com) → **[프로젝트 추가]**

- **프로젝트 이름** — 아무거나. 내 홈 이름을 쓰면 나중에 알아보기 좋습니다
- **Google 애널리틱스** — **꺼도 됩니다.** 방문자 통계를 Firebase에서 보고 싶은 게 아니라면 필요 없습니다

#### ② Authentication (로그인 기능)

왼쪽 메뉴 **Authentication(인증)** → **[시작하기 / Get started]**
→ **로그인 방법(Sign-in method)** 탭 → **이메일/비밀번호(Email/Password)** 선택
→ **사용 설정(Enable)** 을 켜고 **[저장 / Save]**

- 그 아래 「이메일 링크(비밀번호 없는 로그인) / Email link (passwordless sign-in)」는 **끈 채로** 둡니다
- 이걸 안 켜면 나중에 **관리자 계정을 만들 수 없습니다**

#### ③ Firestore Database (글·설정이 저장되는 곳)

왼쪽 메뉴 **Firestore Database** → **[데이터베이스 만들기 / Create database]**

**에디션(Edition) — Standard(표준)를 고르세요.**

| | Standard | Enterprise |
|---|---|---|
| 이 홈에 필요한 기능 | **전부 있음** | 전부 있음 |
| 추가되는 것 | — | MongoDB 호환 등 대규모 서비스용 |
| 비용 | 무료 한도 있음 | 더 비쌈 |

개인홈이 쓰는 기능은 Standard에 전부 들어 있습니다. Enterprise는 회사에서 대규모로 쓸 때의 선택지라 고를 이유가 없습니다.

**위치(Location) — `asia-northeast3` (Seoul)** 을 고르세요.

Firestore는 **어느 지역을 골라도 무료 한도가 같습니다.** 그러니 가장 가까운 서울이 유리합니다. 글 목록·설정을 읽어오는 속도가 여기서 결정됩니다.

> ⚠️ **위치는 나중에 바꿀 수 없습니다.** 잘못 골랐다면 새 프로젝트를 만들고 환경설정의 **데이터베이스 이전** 기능으로 옮겨야 합니다.

**보안 규칙 — 「프로덕션 모드에서 시작 / Start in production mode」** 을 고르세요.

테스트 모드는 30일 동안 **누구나 내 데이터를 읽고 쓸 수 있는 상태**이고, 30일이 지나면 갑자기 전부 막힙니다. 어차피 다음 단계에서 제대로 된 규칙을 붙여넣으니 잠긴 채로 시작하는 게 안전합니다.

#### ④ Storage (이미지가 저장되는 곳)

왼쪽 메뉴 **Storage** → **[시작하기 / Get started]**

여기서 **요금제를 Blaze(종량제)로 바꾸라고 할 수 있습니다.** 카드 등록이 필요하지만, **무료 한도(5GB) 안에서는 0원이 청구**됩니다. 걱정되면 아래 ⑥의 예산 알림을 꼭 걸어 두세요.

**위치(Location) — 여기가 진짜 고민되는 지점입니다.** 무료 한도가 **미국 리전에만** 적용되기 때문입니다.

| 목표 | 고를 위치 | 한국에서 지연 | 예상 비용 |
|---|---|---:|---|
| **무료로 쓰고 싶다** | `us-west1` (오레곤) | 110~130ms | **0원** (5GB까지) |
| **속도가 중요하다** | `asia-northeast3` (서울) | 5~15ms | 월 **1,000원** 안팎 |

- **무료를 고르실 거면 반드시 `us-west1`** 입니다. 콘솔이 기본으로 제안하는 `us-central1`(아이오와)보다 40ms쯤 빠릅니다
- 미국을 골라도 **글·목록은 서울(Firestore)에서 오므로 페이지는 즉시 뜨고, 사진만 조금 늦게 채워집니다**
- 한 번 받은 이미지는 브라우저에 저장되므로 **다시 볼 때는 차이가 없습니다**
- 그림이 많고 방문자도 꾸준하다면 서울이 낫습니다. 5GB 보관에 월 160원 정도이고, 나머지는 사람들이 이미지를 얼마나 받아 가느냐에 따라 붙습니다

> ⚠️ **Storage 위치도 나중에 못 바꿉니다.** Firestore와 **별개로** 고르는 항목이라, 여기서 미국을 골라도 Firestore는 서울 그대로입니다.

> **「기본 리소스 위치 / Default GCP resource location」 를 정하라는 화면이 뜬다면** 그게 바로 이 선택입니다.
> 이름만 다를 뿐 같은 값이고, **프로젝트에 한 번만 정해지며 되돌릴 수 없습니다.** 위 표에서 고른 값을 그대로 넣으세요.

보안 규칙은 Firestore와 마찬가지로 **프로덕션 모드(Start in production mode)** 로 시작합니다.

#### ⑤ 웹 앱 등록 — 연결 정보 받기

> ⚠️ **「앱 호스팅 / App Hosting」 메뉴가 아닙니다.**
> 왼쪽 메뉴의 **App Hosting(앱 호스팅)** 은 **홈을 Firebase에서 직접 굴리는 기능**입니다.
> 우리 홈은 이미 Vercel에 올라가 있으니 **들어가지 마세요.** 리전을 묻거나 GitHub 연결을
> 요구하는 화면이 나왔다면 잘못 들어온 것이니 뒤로 나오시면 됩니다.
> 우리가 할 일은 **연결 정보(firebaseConfig)를 받아 오는 것뿐**이고, 그건 아래 경로에 있습니다.

**정확한 경로 (콘솔 언어가 한글이든 영문이든 이 순서입니다)**

```
⚙️ 프로젝트 설정 (Project settings)
   └ 일반 (General) 탭
      └ 페이지 맨 아래  내 앱 (Your apps)
         └ [앱 추가 / Add app]  →  웹 / Web  ( </> 아이콘 )
```

왼쪽 메뉴 맨 위 **프로젝트 개요(Project Overview)** 옆의 **⚙️ 톱니바퀴**를 누르면 **프로젝트 설정(Project settings)** 이 나옵니다. 거기 **일반(General)** 탭에서 **맨 아래까지 스크롤**하면 **내 앱(Your apps)** 이 있습니다.

> 프로젝트를 막 만들었다면 **프로젝트 개요(Project Overview)** 첫 화면 가운데에 iOS · Android · **`</>`** · Unity 아이콘이 나란히 뜨기도 합니다. 그 `</>` 를 눌러도 같은 화면입니다.

**등록 화면에서 고를 것**

| 항목 (한글 / English) | 어떻게 |
|---|---|
| 앱 닉네임 / App nickname | 아무거나 — 내부에서만 보입니다 |
| Firebase 호스팅 설정 / Also set up Firebase Hosting | **체크하지 마세요** — Vercel을 쓰므로 불필요 |

**여기서는 리전을 묻지 않습니다.** 웹 앱 등록은 「이 주소로 접속하는 앱이 있다」고 알려 주는 절차라 저장 위치와 무관합니다. 리전은 앞의 ③ Firestore와 ④ Storage에서 이미 정했습니다.

**[앱 등록 / Register app]** 을 누르면 `const firebaseConfig = { ... }` 코드가 나옵니다.
**그 코드를 통째로 복사해 설치 화면에 붙여넣으면 값이 자동으로 채워집니다.** 하나씩 옮겨 적을 필요 없습니다.

> **코드를 놓쳤다면** — 위와 같은 경로(**프로젝트 설정 → 일반 → 내 앱**)에서 만든 웹 앱을 고르고
> **SDK 설정 및 구성 (SDK setup and configuration)** 에서 **구성 (Config)** 을 선택하면 같은 코드가 다시 나옵니다.
> 언제든 다시 볼 수 있으니 앱을 새로 만들 필요 없습니다.

#### ⑥ 보안 규칙 붙여넣기 + 마무리

1. 설치 화면의 **[Firestore 규칙 복사]** → 콘솔 **Firestore Database → 규칙** 탭에 붙여넣고 **[게시]**
2. 설치 화면의 **[Storage 규칙 복사]** → 콘솔 **Storage → 규칙** 탭에 붙여넣고 **[게시]**
   - 이 두 규칙이 공개범위(전체공개/회원만/비공개)와 관리자 권한을 실제로 지켜 줍니다
3. 설치 화면 **[연결 확인]**
4. **관리자 계정 만들기** — 여기서 만드는 첫 계정이 이 홈의 관리자가 됩니다

**요금 안전장치 (Blaze로 바꿨다면 권장)**

Firebase 콘솔 좌하단 **[요금제 업그레이드 / 사용량 및 청구]** → **예산 알림**에서 5,000원쯤에 알림을 걸어 두세요. 종량제는 상한선이 없어서, 갑자기 유입이 몰릴 때 알아채기 위한 안전장치입니다.

### 마지막 단계 (공통) — 방문자에게도 보이게

여기까지 하면 **설정한 브라우저에서만** 연결된 상태입니다. 둘 중 편한 쪽으로 한 번만 등록하면 끝입니다.

**방법 1 — 파일 올리기 (터미널 없이, 권장)**

1. 설치 화면에서 **[① 설정 파일 내려받기]**
2. 같은 화면의 입력칸에 **내 저장소 주소**(`https://github.com/내계정/저장소`)를 넣으면
   **[GitHub 업로드 페이지 열기]** 버튼이 생깁니다
3. 열린 화면에 파일을 **끌어다 놓고 [Commit changes]** — 1~2분 뒤 자동 재배포되며 반영됩니다

**방법 2 — Vercel 환경변수**

설치 화면의 **[환경변수 복사]** → Vercel 프로젝트 → **Settings → Environment Variables**에 붙여넣고 재배포.

> 두 값 모두 원래 브라우저에 공개되는 값이라 노출돼도 안전합니다. 실제 보안은 DB 규칙이 담당합니다.

---

## 4. 권한 — 누가 무엇을 볼 수 있나

설치 때 적용한 규칙이 아래를 강제합니다. 화면에서 고른 공개범위가 서버에서도 그대로 지켜집니다.

| 대상 | 규칙 |
|---|---|
| 전체공개 글 | 누구나 읽기 |
| 멤버공개 글 | 로그인한 회원만 |
| 나만보기 | 작성자와 관리자만 |
| 수정 | 작성자 본인(또는 관리자) |
| 삭제 | 작성자 본인 또는 관리자 |
| 방명록 | 비로그인 방문자도 남길 수 있음 |
| 사이트 설정 | 읽기는 누구나, 변경은 관리자만 |
| 이미지 | 보기는 누구나, 올리기는 로그인 회원 |

회원 가입은 **가입코드** 방식입니다(환경설정 → 회원/보안에서 변경, 기본 `WELCOME`).

---

## 5. 운영

### 기존에 꾸며 둔 홈이 있다면

브라우저에서 이미 테마·메뉴를 꾸며 둔 상태로 DB를 연결했다면,
**환경설정 → 데이터 백업 → [↑ 설정 올리기]** 를 한 번 누릅니다.
테마·폰트·메뉴·로고·메인 위젯 배치가 서버로 올라가 방문자에게도 같은 모습으로 보입니다.

### 백업

**환경설정 → 데이터 백업**

| 버튼 | 담기는 것 |
|---|---|
| ↓ 데이터만 | 글·캐릭터·자관·설정 + 이미지 전부 |
| ↓ 회원까지 | 위 + 회원 태그·가입코드 등 |

서버 모드에서는 **DB와 이미지 저장소에서 직접** 받아옵니다.
복원하면 이미지가 새 저장소로 올라가고 **글 안의 이미지 주소도 자동으로 바뀝니다.**

#### ⚠️ Firebase를 쓴다면 — 백업 전에 한 번만 해 둘 것

Firebase Storage는 **다른 주소에서 파일을 읽어 가는 것을 기본적으로 막습니다.**
홈에서 **보는 것은 아무 문제 없지만**, 백업 zip을 만들거나 데이터베이스를 이전할 때는
파일을 직접 받아와야 해서 이 설정이 없으면 **이미지가 빠진 채로 백업됩니다.**

한 번만 열어 주면 됩니다. 프로그램을 깔 필요 없이 브라우저에서 끝납니다.

1. [Google Cloud Console](https://console.cloud.google.com)에 접속 (Firebase와 같은 계정)
2. 위쪽에서 **내 Firebase 프로젝트**를 고르고, 오른쪽 위 **`>_` (Cloud Shell 활성화)** 클릭
3. 아래로 나타나는 검은 창에 두 줄을 붙여넣고 Enter

```bash
echo '[{"origin":["*"],"method":["GET"],"maxAgeSeconds":3600}]' > cors.json
```

```bash
gcloud storage buckets update gs://내버킷이름 --cors-file=cors.json
```

`내버킷이름`은 설치할 때 넣은 **storageBucket** 값입니다 (`○○○.firebasestorage.app` 모양).
`Updated bucket …` 이 나오면 끝입니다.

> 여기서 여는 것은 **읽기(GET)뿐**이고, 이미 공개된 이미지라 새로 위험해지는 것은 없습니다.
> Supabase는 기본으로 열려 있어 이 작업이 필요 없습니다.

#### 쓰지 않는 이미지 정리

글을 지워도 **이미지는 저장소에 남습니다.** 같은 이미지를 다른 글이 쓰고 있을 수 있어
글 삭제와 함께 지우면 위험하기 때문입니다.

**환경설정 → 데이터 백업 → 쓰지 않는 이미지 정리 → [찾아보기]** 를 누르면
지금 어디에서도 참조하지 않는 파일만 골라 개수와 용량을 보여 줍니다. 확인하고 지우면 됩니다.
무료 용량(5GB)이 차오르면 여기부터 보세요.

### 데이터베이스 이전

**환경설정 → 데이터 백업 → 데이터베이스 이전**

새 프로젝트나 다른 서비스(Supabase ↔ Firebase)의 연결 정보를 넣고 [연결 확인] → [이전 시작].
글·설정·이미지가 통째로 복사되고, 끝나면 새 `ohome.config.json`을 내려받아 저장소에 올리면 됩니다.

> 옮기는 곳도 스키마(또는 보안 규칙)를 미리 적용해 두어야 합니다 — 설치 화면과 같은 내용입니다.
> **회원 계정(비밀번호)은 옮겨지지 않습니다.** 계정은 각 서비스가 관리하므로 새 곳에서 다시 가입해야 합니다.

### 초기화

**RESET** → 지울 항목을 골라서. 메뉴별 데이터 18종 + 메인 구성 / 사이트 설정 / 이미지 / 회원 목록.
서버 모드에서는 **DB의 글과 Storage의 이미지까지 실제로 지워집니다.** 끝나면 몇 건을 지웠는지 알려 줍니다.

#### 로그인 계정은 홈에서 지울 수 없습니다

「회원 목록」을 초기화하면 **홈에 보이는 회원 목록**은 비워지지만,
**로그인 계정 자체는 남아 있어 그 사람은 계속 로그인할 수 있습니다.**

계정 삭제는 관리자 권한이 있어야 하는 작업이라 홈(공개된 웹페이지)에서는 할 수 없습니다.
할 수 있게 만들려면 관리자 키를 홈에 넣어야 하는데, 그러면 **누구나 남의 계정을 지울 수 있게 됩니다.**

계정은 콘솔에서 지웁니다.

| 서비스 | 경로 |
|---|---|
| Firebase | 콘솔 → **Authentication → Users** → 계정 선택 → 휴지통 (여러 개 선택 가능) |
| Supabase | 대시보드 → **Authentication → Users** → 계정 오른쪽 ⋯ → Delete user |

> 반대로, 콘솔에서 계정만 지우고 홈의 회원 목록을 그대로 두면 목록에 이름이 남습니다.
> **환경설정 → 회원/보안**에서 그 회원의 [DELETE]를 눌러 목록에서도 지워 주세요.

---

## 6. 배포본 다시 만들기 (배포하는 사람용)

```bash
npm run pack
```

`dist/ohome-<버전>-<날짜>.zip`이 만들어집니다.
`node_modules`·`.next`·`.git`과 **연결 정보 파일(`ohome.config.json`)·`.env`는 자동으로 빠집니다.**

---

## 7. 문제가 생기면

| 증상 | 확인 |
|---|---|
| `npm run build` 실패 | Node 20.9 이상인지 (`node -v`) |
| 설치 화면에서 "스키마가 아직 없습니다" | SQL을 SQL Editor에서 실행했는지 |
| Firebase에서 "보안 규칙이 아직 적용되지 않았습니다" | Firestore·Storage 규칙을 각각 게시했는지 |
| Firebase에서 "응답이 없습니다" | projectId가 맞는지, Firestore 데이터베이스를 만들었는지 |
| 로그인 창에서 "이메일/비밀번호" 오류 | Firebase는 Authentication에서 이메일/비밀번호를 켜야 합니다 |
| 방문자에게 빈 홈으로 보임 | `public/ohome.config.json`을 저장소에 올렸는지 |
| 방문자에게 기본 테마로 보임 | 데이터 백업 탭에 「서버에 올리지 않은 설정 N건」이 떠 있으면 눌러서 올리기 (로컬로 먼저 꾸민 경우에만 생깁니다) |
| 글은 보이는데 이미지가 안 보임 | 이미지 저장소(Storage) 규칙이 게시됐는지 |
| 홈이 전반적으로 느림 | **Function Region이 Seoul인지** (2-B ④) · `x-vercel-id`가 `iad1::…`이면 아직 미국입니다 |
| 백업 zip에 이미지가 없음 | Firebase는 저장소 CORS를 열어야 합니다 (5장 「백업」) |
| 콘솔에 `blocked by CORS policy` | 위와 같은 원인입니다 |
| 저장소 용량이 계속 차오름 | 지운 글의 이미지가 남아 있을 수 있습니다 — 「쓰지 않는 이미지 정리」 |
| 이미지만 유난히 느림 | Firebase Storage를 미국 리전(`us-west1`)에 만든 경우 — 무료 한도 때문이라 정상입니다 |
| 관리자 계정 만들기가 끝나지 않음 | Firestore 데이터베이스를 만들었는지 · 이름이 `(default)`인지 |
| "이미 사용 중인 이메일" | 앞선 시도에서 계정만 만들어진 상태 — 같은 비밀번호로 다시 누르면 이어집니다 |
| Supabase가 며칠 뒤 멈춤 | 무료 플랜은 7일 무접속 시 일시정지 — 콘솔에서 재개하거나 주기적으로 접속 |
| 환경설정이 모바일에서 안 열림 | 의도된 동작 — 환경설정은 PC 전용 |

---

## 8. 체크리스트

- [ ] `node -v` → 20.9 이상
- [ ] `npm run build` 통과
- [ ] GitHub 저장소 Private 여부 결정
- [ ] Vercel Import → Deploy
- [ ] 배포 주소 접속 → 설치 화면에서 DB 선택·연결·스키마(규칙) 적용
- [ ] 관리자 계정 만들기
- [ ] `ohome.config.json`을 `public/`에 올리고 푸시
- [ ] (기존 홈이 있었다면) [↑ 설정 올리기]
- [ ] 로그아웃 상태(다른 브라우저·시크릿창)로 접속해 방문자 화면 확인
- [ ] 백업 zip 한 번 받아 보관
